selection:
  # Custom-boundary mode: 100,000 ha square subset centered on TSA29 centroid
  boundary_path: "data/tsa_boundaries.gpkg"
  boundary_layer: null
  boundary_code: "29mini"

  # Optional strata labels for targeted runs (informational in current flow).
  strata: []

modes:
  resume: false
  dry_run: false
  verbose: true
  skip_checks: false
  debug_rows: null

run:
  run_id: "tsa29mini_20260724"
  log_dir: "runtime/logs"

# Notes:
# This instance is a spatial subset of the full TSA29 model.
# Stage 00 (VRI prep) and Stage 01 (VDYP/TIPSY growth) are skipped —
# the compiled yield curves and fragment geometry are already present.
# The model is ready for Patchworks export and scenario runs.
